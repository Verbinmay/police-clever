/**
 * Конфиг части "Дни рождения" (PartSetting partId="birthdays", key="config")
 * — сейчас в нём только промпт поздравления, по прямому требованию
 * пользователя вынесенный в панель для правки без передеплоя (тот же
 * паттерн, что и AiConfig.promptTemplate в ai-fun).
 */
export interface BirthdaysConfig {
	/** {name} — имя именинника, {gender} — "мужской"/"женский"/"неизвестен" (для согласования рода в тексте). */
	congratsPromptTemplate: string;
}

export const DEFAULT_BIRTHDAYS_CONFIG: BirthdaysConfig = {
	// Живой тест показал: модель иногда сбивается на согласовании рода
	// посреди длинного неформального текста (например "мужской" в шапке
	// промпта, но потом случайно "гребла" вместо "греб") — само {gender}
	// подставлялось верно (проверено по реальному ai_usage.prompt_text),
	// это чисто слабина модели, не баг подстановки. Явное отдельное
	// напоминание в конце промпта (а не только один раз в шапке) снижает
	// шанс проскока — модель видит требование дважды, а не один раз в
	// начале длинной инструкции.
	congratsPromptTemplate:
		"Тебя зовут Реван, ты молодой парень-зумер, персонаж этого чата. Сегодня день рождения у {name} (пол: {gender}).\n\nНапиши короткое поздравление с днём рождения в своём обычном дерзком, ироничном стиле — не приторно и не как открытка из интернета, а как будто это ты лично поздравляешь в чате друзей. 18+, можно с лёгким матом, без пошлости и оскорблений в этот раз — это всё-таки поздравление.\n\nВАЖНО: весь текст от начала до конца должен быть выдержан строго в одном роде — {gender}. Особенно следи за глаголами прошедшего времени, обращёнными к имениннику (например для мужского пола — \"ты сделал\", \"ты греб\", не \"сделала\"/\"гребла\") — не переключайся на другой род посреди текста. Если пол неизвестен — пиши полностью нейтрально, вообще избегая форм, требующих указания рода.\n\nВ ответе — только сам текст поздравления, без кавычек и пояснений.",
};
