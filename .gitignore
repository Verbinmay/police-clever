# dependencies
/node_modules

# env — широкий паттерн специально: любой файл с "env" в имени, кроме
# .env.example (шаблон без секретов), не должен случайно утечь в git.
*env*
!.env.example

# личный план/состояние — не для гита и не для деплоя, только локально
STATE.md

# OS
.DS_Store

# logs
logs
*.log

# runtime data
pids
*.pid
*.seed
*.pid.lock
